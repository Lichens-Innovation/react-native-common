import { notImplementedYet } from '../utils/platform.utils';

export const useAnimateOnFocus = () => {
  notImplementedYet(
    '[Hooks] useAnimateOnFocus — focus-driven list animation is not implemented on web (no-op returns true)'
  );
  return true;
};
