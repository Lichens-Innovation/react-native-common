import { notImplementedYet } from '../utils/platform.utils';

export const useUpdates = () => {
  notImplementedYet('[OTA] useUpdates — expo-updates / EAS OTA flow is not wired on web (hook is a no-op)');
};
