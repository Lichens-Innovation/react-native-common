import { notImplementedYet } from '../utils/platform.utils';

export const useImagePicker = () => {
  notImplementedYet(
    '[Hooks] useImagePicker — image library picker via expo-image-picker is not implemented on Windows desktop',
  );
};
