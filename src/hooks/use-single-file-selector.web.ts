import { notImplementedYet } from '../utils/platform.utils';

export const useSingleFileSelector = () => {
  notImplementedYet(
    '[Hooks] useSingleFileSelector — file selection UI backed by expo-document-picker is not implemented on web'
  );
};
